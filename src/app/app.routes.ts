import { Routes } from '@angular/router';
import { routes as appRoutes } from './app-routing-module'; // Import routes from AppRoutingModule

export const routes: Routes = appRoutes; // Export the imported routes
